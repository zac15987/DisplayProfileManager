<Window x:Class="DisplayProfileManager.UI.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DisplayProfileManager.UI.Windows"
        xmlns:converters="clr-namespace:DisplayProfileManager.UI.Converters"
        mc:Ignorable="d"
        Title="Settings" Height="600" Width="500" MinHeight="500" MinWidth="450"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        WindowStyle="SingleBorderWindow"
        Loaded="Window_Loaded">
    
    <WindowChrome.WindowChrome>
        <WindowChrome 
            CaptionHeight="32"
            ResizeBorderThickness="0"
            CornerRadius="0"
            GlassFrameThickness="0,32,0,0"
            UseAeroCaptionButtons="True" />
    </WindowChrome.WindowChrome>
    
    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonHoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonPressedBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryButtonForegroundBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryButtonHoverBackgroundBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryButtonPressedBackgroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource DangerButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource DangerButtonHoverBackgroundBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource DangerButtonHoverBackgroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource TextBoxBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource TextBoxBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>


        <Style x:Key="ModernCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style x:Key="HeaderTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock" BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="LabelTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="WindowControlButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource WindowControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlPressedBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowControlButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource CloseButtonHoverBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource CloseButtonHoverBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{DynamicResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <!-- Title bar area (32px normal, 40px maximized) -->
            <RowDefinition Name="TitleBarRowDefinition" Height="32"/>
            <!-- Main content area -->
            <RowDefinition Height="*"/>
            <!-- Footer area -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar Area -->
        <Grid Grid.Row="0" Name="TitleBarGrid" Background="{DynamicResource TitleBarBackgroundBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- App Title -->
            <TextBlock Grid.Column="0" Text="Settings" 
                       VerticalAlignment="Center" 
                       Margin="12,0,0,0"
                       FontFamily="Segoe UI" 
                       FontSize="12" 
                       Foreground="{DynamicResource TitleBarTextBrush}"/>
            
            <!-- Window Control Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <!-- Minimize Button -->
                <Button Name="MinimizeButton" 
                        Content="&#xE921;" 
                        Style="{StaticResource WindowControlButtonStyle}"
                        Click="MinimizeButton_Click"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        ToolTip="Minimize"/>
                
                <!-- Close Button -->
                <Button Name="CloseButton" 
                        Content="&#xE8BB;" 
                        Style="{StaticResource WindowCloseButtonStyle}"
                        Click="CloseButton_Click"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        ToolTip="Close"/>
            </StackPanel>
        </Grid>

        <!-- Main Content Area -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="16,16,16,0">
                <StackPanel Margin="8">
                    <!-- General Section -->
                    <Border Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="General" Style="{StaticResource SectionHeaderStyle}"/>
                            
                            <TextBlock Text="Theme" Style="{StaticResource LabelTextBlockStyle}"/>
                            <ComboBox Name="ThemeComboBox" Style="{StaticResource ModernComboBoxStyle}" 
                                      SelectionChanged="ThemeComboBox_SelectionChanged">
                                <ComboBoxItem Content="System" Tag="System"/>
                                <ComboBoxItem Content="Light" Tag="Light"/>
                                <ComboBoxItem Content="Dark" Tag="Dark"/>
                            </ComboBox>
                            
                            <TextBlock Text="Language" Style="{StaticResource LabelTextBlockStyle}" Margin="0,12,0,4"/>
                            <ComboBox Name="LanguageComboBox" Style="{StaticResource ModernComboBoxStyle}" 
                                      SelectionChanged="LanguageComboBox_SelectionChanged">
                                <ComboBoxItem Content="English (US)" Tag="en-US"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                    <!-- Startup Section -->
                    <Border Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Startup" Style="{StaticResource SectionHeaderStyle}"/>
                            
                            <CheckBox Name="StartWithWindowsCheckBox" Content="Start with Windows" 
                                      Style="{StaticResource ModernCheckBoxStyle}"
                                      Checked="StartWithWindowsCheckBox_Changed"
                                      Unchecked="StartWithWindowsCheckBox_Changed"/>
                            
                            <CheckBox Name="StartInSystemTrayCheckBox" Content="Start in system tray when Windows starts" 
                                      Style="{StaticResource ModernCheckBoxStyle}" Margin="20,4,0,0"
                                      Checked="StartInSystemTrayCheckBox_Changed"
                                      Unchecked="StartInSystemTrayCheckBox_Changed"/>
                            
                            <TextBlock Text="Startup Profile" Style="{StaticResource LabelTextBlockStyle}" Margin="0,12,0,4"/>
                            <ComboBox Name="StartupProfileComboBox" Style="{StaticResource ModernComboBoxStyle}"
                                      SelectionChanged="StartupProfileComboBox_SelectionChanged">
                                <ComboBoxItem Content="None" Tag=""/>
                            </ComboBox>
                            
                            <CheckBox Name="ApplyStartupProfileCheckBox" Content="Apply startup profile on launch" 
                                      Style="{StaticResource ModernCheckBoxStyle}" Margin="0,8,0,0"
                                      IsEnabled="{Binding ElementName=StartupProfileComboBox, Path=SelectedIndex, Converter={x:Static converters:BooleanToVisibilityConverter.Instance}}"
                                      Checked="ApplyStartupProfileCheckBox_Changed"
                                      Unchecked="ApplyStartupProfileCheckBox_Changed"/>
                        </StackPanel>
                    </Border>

                    <!-- Window Behavior Section -->
                    <Border Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Window Behavior" Style="{StaticResource SectionHeaderStyle}"/>
                            
                            
                            <TextBlock Text="When closing the application:" Style="{StaticResource LabelTextBlockStyle}" Margin="0,12,0,4"/>
                            <StackPanel Margin="20,0,0,0">
                                <RadioButton Name="CloseToTrayRadio" Content="Minimize to system tray" 
                                             Style="{StaticResource ModernRadioButtonStyle}"
                                             GroupName="CloseAction"
                                             Checked="CloseActionRadio_Changed"/>
                                <RadioButton Name="ExitApplicationRadio" Content="Exit application" 
                                             Style="{StaticResource ModernRadioButtonStyle}"
                                             GroupName="CloseAction"
                                             Checked="CloseActionRadio_Changed"/>
                            </StackPanel>
                            
                            <CheckBox Name="RememberCloseChoiceCheckBox" Content="Remember my choice" 
                                      Style="{StaticResource ModernCheckBoxStyle}" Margin="20,8,0,0"
                                      Checked="RememberCloseChoiceCheckBox_Changed"
                                      Unchecked="RememberCloseChoiceCheckBox_Changed"/>
                        </StackPanel>
                    </Border>

                    <!-- Notifications Section -->
                    <Border Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Notifications" Style="{StaticResource SectionHeaderStyle}"/>
                            
                            <CheckBox Name="ShowNotificationsCheckBox" Content="Show notifications when profiles are applied" 
                                      Style="{StaticResource ModernCheckBoxStyle}"
                                      Checked="ShowNotificationsCheckBox_Changed"
                                      Unchecked="ShowNotificationsCheckBox_Changed"/>
                        </StackPanel>
                    </Border>

                    <!-- Global Hotkeys Section -->
                    <Border Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Global Hotkeys" Style="{StaticResource SectionHeaderStyle}"/>
                            
                            <TextBlock Text="Hotkeys are configured individually for each profile in the Profile Editor. Each hotkey can be enabled or disabled per profile." 
                                       Style="{StaticResource ModernTextBlockStyle}" 
                                       FontSize="12" 
                                       Foreground="{DynamicResource TertiaryTextBrush}" 
                                       TextWrapping="Wrap" 
                                       Margin="0,0,0,8"/>
                            
                            <!-- Read-only list of configured hotkeys -->
                            <TextBlock Text="Configured Hotkeys:" Style="{StaticResource LabelTextBlockStyle}" Margin="0,12,0,4"/>
                            <Border Background="{DynamicResource TextBoxBackgroundBrush}" 
                                    BorderBrush="{DynamicResource TextBoxBorderBrush}" 
                                    BorderThickness="1" 
                                    CornerRadius="3" 
                                    MinHeight="80" 
                                    MaxHeight="120">
                                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8">
                                    <StackPanel Name="HotkeyListPanel">
                                        <TextBlock Text="No hotkeys configured" 
                                                   Style="{StaticResource ModernTextBlockStyle}" 
                                                   FontSize="12" 
                                                   Foreground="{DynamicResource TertiaryTextBrush}" 
                                                   FontStyle="Italic"/>
                                    </StackPanel>
                                </ScrollViewer>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Updates Section -->
                    <Border Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Updates" Style="{StaticResource SectionHeaderStyle}"/>
                            
                            <CheckBox Name="CheckForUpdatesCheckBox" Content="Automatically check for updates" 
                                      Style="{StaticResource ModernCheckBoxStyle}"
                                      Checked="CheckForUpdatesCheckBox_Changed"
                                      Unchecked="CheckForUpdatesCheckBox_Changed"/>
                        </StackPanel>
                    </Border>

                    <!-- About Section -->
                    <Border Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="About" Style="{StaticResource SectionHeaderStyle}"/>
                            
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Version: " Style="{StaticResource ModernTextBlockStyle}"/>
                                <TextBlock Name="VersionTextBlock" Text="1.0.0" Style="{StaticResource ModernTextBlockStyle}"/>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Text="Settings Location: " Style="{StaticResource ModernTextBlockStyle}" FontSize="12" Foreground="{DynamicResource TertiaryTextBrush}"/>
                                <TextBlock Name="SettingsPathTextBlock" Style="{StaticResource ModernTextBlockStyle}" 
                                           FontSize="12" Foreground="{DynamicResource TertiaryTextBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                            
                            <!-- Community Features Section -->
                            <TextBlock Text="Community Features:" Style="{StaticResource ModernTextBlockStyle}" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <TextBlock Style="{StaticResource ModernTextBlockStyle}" FontSize="12" Foreground="{DynamicResource TertiaryTextBrush}" TextWrapping="Wrap">
                                <Run Text="Audio device switching suggested by"/>
                                <Hyperlink Name="CatriksLink" NavigateUri="https://github.com/Catriks" RequestNavigate="Hyperlink_RequestNavigate" Foreground="{DynamicResource LinkBrush}">
                                    <Run Text="@Catriks"/>
                                </Hyperlink>
                                <Run Text=" and"/>
                                <Hyperlink Name="AlienmarioLink" NavigateUri="https://github.com/Alienmario" RequestNavigate="Hyperlink_RequestNavigate" Foreground="{DynamicResource LinkBrush}">
                                    <Run Text="@Alienmario"/>
                                </Hyperlink>
                                <Run Text=" ("/>
                                <Hyperlink Name="IssueLink" NavigateUri="https://github.com/zac15987/DisplayProfileManager/issues/1" RequestNavigate="Hyperlink_RequestNavigate" Foreground="{DynamicResource LinkBrush}">
                                    <Run Text="Issue #1"/>
                                </Hyperlink>
                                <Run Text=")"/>
                            </TextBlock>
                            
                            <!-- Libraries Section -->
                            <TextBlock Text="Key Libraries:" Style="{StaticResource ModernTextBlockStyle}" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <StackPanel Margin="0,0,0,4">
                                <TextBlock Style="{StaticResource ModernTextBlockStyle}" FontSize="12" Foreground="{DynamicResource TertiaryTextBrush}">
                                    <Hyperlink Name="AudioSwitcherLink" NavigateUri="https://github.com/xenolightning/AudioSwitcher" RequestNavigate="Hyperlink_RequestNavigate" Foreground="{DynamicResource LinkBrush}">
                                        <Run Text="AudioSwitcher.AudioApi"/>
                                    </Hyperlink>
                                    <Run Text=" - Audio device management"/>
                                </TextBlock>
                                <TextBlock Style="{StaticResource ModernTextBlockStyle}" FontSize="12" Foreground="{DynamicResource TertiaryTextBrush}">
                                    <Hyperlink Name="NewtonsoftLink" NavigateUri="https://www.newtonsoft.com/json" RequestNavigate="Hyperlink_RequestNavigate" Foreground="{DynamicResource LinkBrush}">
                                        <Run Text="Newtonsoft.Json"/>
                                    </Hyperlink>
                                    <Run Text=" - JSON serialization"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
        </ScrollViewer>

        <!-- Footer Area -->
        <Border Grid.Row="2" Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="16,12" Margin="16,0,16,16">
            <Button Name="ResetSettingsButton" Content="Reset All Settings" 
                    Style="{StaticResource DangerButtonStyle}" 
                    HorizontalAlignment="Right"
                    Click="ResetSettingsButton_Click"/>
        </Border>
    </Grid>
</Window>