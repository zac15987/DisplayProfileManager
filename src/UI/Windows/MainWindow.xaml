<Window x:Class="DisplayProfileManager.UI.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DisplayProfileManager.UI.Windows"
        xmlns:converters="clr-namespace:DisplayProfileManager.UI.Converters"
        mc:Ignorable="d"
        Title="Display Profile Manager" Height="600" Width="900" MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize"
        WindowStyle="SingleBorderWindow" 
        Loaded="Window_Loaded">
    
    <WindowChrome.WindowChrome>
        <WindowChrome 
            CaptionHeight="32"
            ResizeBorderThickness="0"
            CornerRadius="0"
            GlassFrameThickness="0,32,0,0"
            UseAeroCaptionButtons="True" />
    </WindowChrome.WindowChrome>
    
    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonHoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonPressedBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryButtonForegroundBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryButtonHoverBackgroundBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryButtonPressedBackgroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource ContentBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <Style x:Key="ModernTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style x:Key="HeaderTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>

        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource WindowControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlPressedBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowControlButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource WindowControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlPressedBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowControlButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource CloseButtonHoverBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource CloseButtonHoverBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{DynamicResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <!-- Title bar area (32px normal, 40px maximized) -->
            <RowDefinition Name="TitleBarRowDefinition" Height="32"/>
            <!-- Main content area -->
            <RowDefinition Height="*"/>
            <!-- Status bar -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar Area -->
        <Grid Grid.Row="0" Name="TitleBarGrid" Background="{DynamicResource TitleBarBackgroundBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- App Icon and Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                <Image Name="AppIconImage" Width="16" Height="16" Margin="0,0,8,0"/>
                <TextBlock Text="Display Profile Manager" 
                           VerticalAlignment="Center"
                           FontFamily="Segoe UI" 
                           FontSize="12" 
                           Foreground="{DynamicResource TitleBarTextBrush}"/>
            </StackPanel>
            
            <!-- Window Control Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <!-- Custom ToTray Button -->
                <Button Name="ToTrayButton" 
                        Content="⬇️" 
                        Style="{StaticResource TitleBarButtonStyle}"
                        Click="ToTrayButton_Click"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        ToolTip="Minimize to Tray"/>
                
                <!-- Minimize Button -->
                <Button Name="MinimizeButton" 
                        Content="&#xE921;" 
                        Style="{StaticResource WindowControlButtonStyle}"
                        Click="MinimizeButton_Click"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        ToolTip="Minimize"/>
                
                <!-- Maximize/Restore Button -->
                <Button Name="MaximizeRestoreButton" 
                        Content="&#xE922;" 
                        Style="{StaticResource WindowControlButtonStyle}"
                        Click="MaximizeRestoreButton_Click"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        ToolTip="Maximize"/>
                
                <!-- Close Button -->
                <Button Name="CloseButton" 
                        Content="&#xE8BB;" 
                        Style="{StaticResource WindowCloseButtonStyle}"
                        Click="CloseButton_Click"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        ToolTip="Close"/>
            </StackPanel>
        </Grid>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Content Header -->
            <Border Grid.Row="0" Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4,4,0,0" Padding="24,16" Margin="0,0,0,0">
                <StackPanel>
                    <TextBlock Text="Display Profile Manager" Style="{StaticResource HeaderTextBlockStyle}"/>
                    <TextBlock Text="Manage display resolution and DPI scaling profiles" 
                               Style="{StaticResource ModernTextBlockStyle}" 
                               Opacity="0.8" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Main Content Grid -->
            <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,1,1" CornerRadius="0,0,4,4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource AlternateBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="16,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="Display Profiles" 
                                       Style="{StaticResource ModernTextBlockStyle}" 
                                       FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Name="AddProfileButton" Content="+ Add Profile" 
                                        Style="{StaticResource ModernButtonStyle}" 
                                        Margin="0,0,8,0" Click="AddProfileButton_Click"/>
                                <Button Name="RefreshButton" Content="🔄" 
                                        Style="{StaticResource SecondaryButtonStyle}" 
                                        Width="32" Height="32" Padding="0" 
                                        Click="RefreshButton_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <ListBox Grid.Row="1" Name="ProfilesListBox" 
                             Style="{StaticResource ModernListBoxStyle}" 
                             BorderThickness="0" 
                             SelectionChanged="ProfilesListBox_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8" Margin="0,2" Background="Transparent">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        
                                        <Grid Grid.Row="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0" Text="{Binding Name}" 
                                                       FontWeight="Medium" FontSize="14"
                                                       Foreground="{DynamicResource PrimaryTextBrush}"/>
                                            
                                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                <TextBlock Text="Active" 
                                                           FontSize="12" Foreground="{DynamicResource SuccessButtonBackgroundBrush}" 
                                                           FontWeight="Medium" 
                                                           Margin="0,0,8,0"
                                                           Visibility="{Binding Path=DataContext.IsActive, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Converter={x:Static converters:BooleanToVisibilityConverter.Instance}}"/>
                                                <TextBlock Text="Default" 
                                                           FontSize="12" Foreground="{DynamicResource ButtonBackgroundBrush}" 
                                                           FontWeight="Medium" 
                                                           Visibility="{Binding IsDefault, Converter={x:Static converters:BooleanToVisibilityConverter.Instance}}"/>
                                            </StackPanel>
                                        </Grid>
                                        
                                        <TextBlock Grid.Row="1" Text="{Binding Description}" 
                                                   FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" 
                                                   TextWrapping="Wrap" Margin="0,2,0,0"/>
                                        
                                        <TextBlock Grid.Row="2" 
                                                   FontSize="11" Foreground="{DynamicResource TertiaryTextBrush}" 
                                                   Margin="0,4,0,0">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0} displays • Modified: {1:MMM d, yyyy}">
                                                    <Binding Path="DisplaySettings.Count"/>
                                                    <Binding Path="LastModifiedDate"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource AlternateBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="16,12">
                        <TextBlock Text="Profile Details" 
                                   Style="{StaticResource ModernTextBlockStyle}" 
                                   FontWeight="SemiBold" FontSize="16"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" Padding="16" VerticalScrollBarVisibility="Auto">
                        <StackPanel Name="ProfileDetailsPanel">
                            <TextBlock Text="Select a profile to view details" 
                                       Style="{StaticResource ModernTextBlockStyle}" 
                                       Foreground="{DynamicResource TertiaryTextBrush}" 
                                       HorizontalAlignment="Center" 
                                       TextAlignment="Center" 
                                       Margin="0,32"/>
                        </StackPanel>
                    </ScrollViewer>

                    <Border Grid.Row="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="16,12">
                        <StackPanel>
                            <Button Name="ApplyProfileButton" Content="Apply Profile" 
                                    Style="{StaticResource ModernButtonStyle}" 
                                    Width="100" HorizontalAlignment="Center" 
                                    Margin="0,0,0,8" Click="ApplyProfileButton_Click"
                                    IsEnabled="False"/>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Name="EditProfileButton" Content="Edit" 
                                        Style="{StaticResource SecondaryButtonStyle}" 
                                        Width="60" Margin="0,0,4,0" 
                                        Click="EditProfileButton_Click"
                                        IsEnabled="False"/>
                                <Button Name="DeleteProfileButton" Content="Delete" 
                                        Style="{StaticResource SecondaryButtonStyle}" 
                                        Width="60" Margin="0,0,4,0" 
                                        Click="DeleteProfileButton_Click"
                                        IsEnabled="False"/>
                                <Button Name="ExportProfileButton" Content="Export" 
                                        Style="{StaticResource SecondaryButtonStyle}" 
                                        Width="60" Margin="0,0,8,0" 
                                        Click="ExportProfileButton_Click"
                                        IsEnabled="False"/>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                                <Button Name="ImportProfileButton" Content="Import Profile" 
                                        Style="{StaticResource SecondaryButtonStyle}" 
                                        Width="120" Margin="0,0,8,0" 
                                        Click="ImportProfileButton_Click"/>
                                <Button Name="OpenProfilesFolderButton" Content="Open Folder" 
                                        Style="{StaticResource SecondaryButtonStyle}" 
                                        Width="100" 
                                        Click="OpenProfilesFolderButton_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{DynamicResource ContentBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="24,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Name="StatusTextBlock" 
                           Text="Ready" 
                           Style="{StaticResource ModernTextBlockStyle}" 
                           FontSize="12" Foreground="{DynamicResource TertiaryTextBrush}" 
                           VerticalAlignment="Center"/>
                
                <Button Grid.Column="1" Name="SettingsButton" Content="Settings" 
                        Style="{StaticResource SecondaryButtonStyle}" 
                        Click="SettingsButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
